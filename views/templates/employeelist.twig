{% extends "../base.twig" %}

{% block style %}
<link rel="stylesheet" href="./liststyle.css">
{% endblock %}

{% block title %}
<title>{{company.name}}</title>
{% endblock %}

{% block header %}
<h2>
				Bienvenue
				{{company.name}}</h2>
			<div class="buttons">
				<form  class="margin" action="/addemployee">
					<button type="submit">Ajouter un employé</button>
				</form>
				<form class=" margin"action="/deconnect">
					<button type="submit">Déconnexion</button>
				</form>
			</div>
            {% endblock %}

{% block main %}

{% for employee in employees %}
{# tableauObjets.sort(function compare(a, b) {
  if (a.nom < b.nom)
     return -1;
  if (a.nom > b.nom )
     return 1;
  return 0;
});
{% if %} #}
				<div class="list">
					<img src="./uploads/{{employee.picture}}">
					<p>{{employee.name}}</p>
					<p>{{employee.post}}</p>
					<p>
					Blame:{{employee.blame}}</p>
					<a href="/employeeupdate/{{employee.id}}">
						<button>
							Modifier</button>
					</a>
					<a href="/employeedelete/{{employee.id}}">
						<button>
							Supprimer</button>
					</a>
					<a href="/blame/{{employee.id}}">
					<button>
						Blamer </button></a>
				</div>
			{% endfor %}
			
{% endblock %}